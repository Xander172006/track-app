 <div className='grid sm:grid-cols-2 sm:grid-rows-6 grid-cols-1 gap-5 place-content-center text-[0.75rem]'>
      {Object.entries(bossValues).map(([bossType, bossValue]) => (
          <div key={bossType} className='w-full bg-gray-800 my-1'>
            <div style={{ width: `${Math.ceil(bossValue / 100)}%` }} className={`h-5 bg-blue-600 relative`}>
              <span className='ml-auto flex justify-end pr-1'>{bossValue}</span>
              <span></span>
            </div>
        </div>
      ))}
    </div>